{{ include('authority/_delete_authority_confirmation.html.twig') }}
<form action="{{ url('authority_delete', {id: authority.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-lg btn-block btn-danger">
        <i class="fa fa-trash" aria-hidden="true"></i>
        {{ 'action.delete_authority'|trans }}
    </button>
</form>

