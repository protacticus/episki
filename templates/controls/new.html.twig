{% extends 'admin/layout.html.twig' %}

{% block body_id 'controls_new' %}

{% block main %}
    <h1>{{ 'title.controls_new'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.number) }}
        {{ form_row(form.requirement) }}
        {{ form_row(form.description) }}
        {{ form_row(form.authorityref) }}
        {{ form_row(form.owners) }}


        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'label.create_controls'|trans }}
        </button>
        {{ form_widget(form.saveAndCreateNew, {label: 'label.save_and_create_new', attr: {class: 'btn btn-primary'}}) }}
        <a href="{{ path('controls_index') }}" class="btn btn-link">
            <i class="fa fa-list-alt" aria-hidden="true"></i> {{ 'action.back_to_list'|trans }}
        </a>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}


{% endblock %}

