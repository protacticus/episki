{{ include('controls/_delete_controls_confirmation.html.twig') }}
<form action="{{ url('controls_delete', {id: controls.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-lg btn-block btn-danger">
        <i class="fa fa-trash" aria-hidden="true"></i>
        {{ 'action.delete_controls'|trans }}
    </button>
</form>

